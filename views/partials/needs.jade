h1 {{message}}

form
	label URL:
	input(type-"text", ng-model="newNeed")
	input.btn.btn-success(type="submit", ng-click='addNeeds(newNeed)', value="I Needs It!")


h4 I Needs to have {{needs.length}} Thingys!!!
.things
	.justOne(ng-repeat="need in needs") 
		img(src='{{need}}')